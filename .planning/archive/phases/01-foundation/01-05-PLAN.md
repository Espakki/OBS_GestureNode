---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: [01-03, 01-04]
files_modified:
  - main.py
  - ui/main_window.py
autonomous: false
requirements: [ENG-05, ENG-06]
must_haves:
  truths:
    - "config.json e carregado e salvo por caminho absoluto derivado de __file__, independente do CWD"
    - "App iniciado de outro CWD (ex: C:\\Windows\\system32) nao cria config.json no CWD"
    - "salvar_config_automatico() debounça em 500ms via instancia QTimer cancelavel"
    - "O save usa tempfile + os.replace() (write atomico), nunca open('config.json','w') direto"
    - "Mover sliders por 10s nao gera multiplos arquivos temporarios nem corrompe config.json"
  artifacts:
    - path: "main.py"
      provides: "Config path absoluto via __file__ e injecao em MainWindow"
      contains: "Path(__file__).parent"
    - path: "ui/main_window.py"
      provides: "Debounce 500ms + write atomico em salvar_config_automatico"
      contains: "os.replace"
  key_links:
    - from: "main.py"
      to: "ui/main_window.py"
      via: "MainWindow(config, config_path=CONFIG_PATH) injeta o Path absoluto"
      pattern: "MainWindow\\(config, config_path="
    - from: "ui/main_window.py salvar_config_automatico"
      to: "config.json"
      via: "QTimer 500ms -> _do_save_config -> os.replace(tmp, self._config_path)"
      pattern: "os\\.replace"
---

<objective>
Corrigir o caminho do config (ENG-05) e tornar o save seguro (ENG-06) — um unico slice vertical "config persiste com seguranca no caminho certo". Hoje: (a) `main.py:12` usa `carregar_config(caminho="config.json")` relativo ao CWD, e `main_window.py:1140` usa `open("config.json", "w")` tambem relativo — entao iniciar o app de um atalho ou de C:\\Windows\\system32 le/escreve no diretorio errado; (b) `salvar_config_automatico()` escreve diretamente no disco a cada um dos 11 callsites (linhas 111, 748, 886, 908, 935, 944, 954, 958, 965, 1013, 1126), sem debounce e sem atomicidade — mover um slider dispara dezenas de writes nao-atomicos, com risco de corrupcao se o processo morrer no meio.

Purpose: ENG-05 + ENG-06 — Roadmap Success Criterion 1 (app via atalho carrega config sem criar arquivo em system32) e Criterion 2 (mover sliders 10s nao gera temp files nem corrompe config). Ambos giram em torno do mesmo metodo de persistencia, por isso ficam no mesmo plano.
Output: `main.py` resolve o path via `Path(__file__).parent` e injeta no `MainWindow`; `salvar_config_automatico()` agenda um `QTimer(self)` cancelavel de 500ms que chama `_do_save_config()`, que escreve via `tempfile.mkstemp` + `os.replace()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@main.py
@ui/main_window.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolver config path via __file__ em main.py e injetar em MainWindow</name>
  <read_first>
    - main.py (arquivo completo — 35 linhas)
    - ui/main_window.py (linhas 96-111 — assinatura de __init__(self, config))
    - .planning/phases/01-foundation/01-RESEARCH.md (secao "Padrao 1: Config Path via __file__", "Code Examples")
    - .planning/phases/01-foundation/01-CONTEXT.md (Claude's Discretion: config path via Path(__file__).parent)
  </read_first>
  <action>
    Em `main.py`:
    1. Adicionar `from pathlib import Path` aos imports (junto a `import json`, linha 2).
    2. Definir constante de modulo `CONFIG_PATH = Path(__file__).parent / "config.json"` (apos a criacao do logger, antes de `carregar_config`). Nota: `main.py` esta na raiz do projeto, entao `Path(__file__).parent` ja aponta para a raiz — NAO usar `.parent.parent` (verificado no RESEARCH.md).
    3. Trocar a assinatura `def carregar_config(caminho="config.json")` para `def carregar_config(caminho=CONFIG_PATH)`. O corpo (try/except) permanece identico — `open(caminho, ...)` continua valido com um Path.
    4. No bloco `if __name__ == "__main__":`, trocar `window = MainWindow(config)` (linha 32) por `window = MainWindow(config, config_path=CONFIG_PATH)`.

    Em `ui/main_window.py`:
    5. Trocar a assinatura `def __init__(self, config):` (linha 96) para `def __init__(self, config, config_path=None):`.
    6. Logo apos `self.config = config or {}` (linha 102), adicionar resolucao do path com fallback robusto:
       - `from pathlib import Path` deve estar nos imports do arquivo (adicionar junto aos imports do topo se ausente — atualmente nao ha import de pathlib).
       - `self._config_path = Path(config_path) if config_path is not None else (Path(__file__).resolve().parent.parent / "config.json")`. O fallback `parent.parent` cobre o caso de MainWindow ser instanciado sem o path (main_window.py esta em ui/, entao parent.parent e a raiz). Isso garante que mesmo sem injecao, o save vai para o lugar certo.
  </action>
  <verify>
    <automated>grep -q 'from pathlib import Path' main.py && grep -q 'Path(__file__).parent / "config.json"' main.py && grep -q 'MainWindow(config, config_path=' main.py && grep -q 'def __init__(self, config, config_path' ui/main_window.py && grep -q 'self._config_path' ui/main_window.py && python -c "import ast; ast.parse(open('main.py',encoding='utf-8').read()); ast.parse(open('ui/main_window.py',encoding='utf-8').read()); print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - main.py importa `Path` e define `CONFIG_PATH = Path(__file__).parent / "config.json"`
    - `carregar_config` usa `CONFIG_PATH` como default
    - main.py instancia `MainWindow(config, config_path=CONFIG_PATH)`
    - MainWindow.__init__ aceita `config_path` e armazena `self._config_path` (Path)
    - main_window.py importa `Path`
    - Ambos os arquivos compilam (ast.parse)
  </acceptance_criteria>
  <done>main.py resolve CONFIG_PATH via Path(__file__).parent e injeta em MainWindow; MainWindow guarda self._config_path com fallback parent.parent; ambos compilam.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar debounce 500ms + write atomico em salvar_config_automatico</name>
  <read_first>
    - ui/main_window.py (linhas 1-5 imports; 96-111 __init__; 1138-1141 salvar_config_automatico atual; e os 11 callsites: 111, 748, 886, 908, 935, 944, 954, 958, 965, 1013, 1126)
    - .planning/phases/01-foundation/01-RESEARCH.md (secao "Padrao 2: Write Atomico", "Padrao 3: Debounce com QTimer", "Common Pitfalls > Pitfall 3", "Code Examples > Exemplo 4", "Don't Hand-Roll")
    - .planning/phases/01-foundation/01-CONTEXT.md (Claude's Discretion: QTimer + write atomico via os.replace)
  </read_first>
  <action>
    Em `ui/main_window.py`:
    1. Imports: adicionar `import tempfile` (ja existem `import json` e `import os` nas linhas 1-2). Adicionar `from PySide6.QtCore import QSize, Qt, QTimer` (a linha 5 atual e `from PySide6.QtCore import QSize, Qt` — incluir `QTimer`).

    2. Em `__init__` (apos `self._config_path` da Task 1, e ANTES de `self._init_config_schema()` na linha 108): criar a instancia de timer cancelavel (Pitfall 3 — NAO usar `QTimer.singleShot` estatico, que e fire-and-forget e nao cancelavel):
       - `self._save_timer = QTimer(self)`
       - `self._save_timer.setSingleShot(True)`
       - `self._save_timer.timeout.connect(self._do_save_config)`

    3. Reescrever `salvar_config_automatico()` (linhas 1138-1141) para APENAS agendar o save com debounce:
       - Corpo: `self._save_timer.start(500)` — `start()` reinicia o timer se ja estava contando, efetivamente debouncing (cada novo callsite cancela o anterior). NAO chamar `_sync_scene_map_from_bindings()` aqui nem escrever no disco aqui.

    4. Criar novo metodo `_do_save_config(self)` (executado pela main thread Qt quando o timer dispara, 500ms apos o ultimo agendamento):
       - Chamar `self._sync_scene_map_from_bindings()` (movido do metodo antigo — preserva o comportamento de sincronizacao de scene_map).
       - Calcular `dir_path = self._config_path.parent`.
       - Write atomico (Padrao 2): `fd, tmp_path = tempfile.mkstemp(dir=dir_path, suffix=".tmp")`; dentro de try, `with os.fdopen(fd, "w", encoding="utf-8") as f: json.dump(self.config, f, indent=4, ensure_ascii=False)` e depois `os.replace(tmp_path, str(self._config_path))`. Em except, remover o temp (`os.unlink(tmp_path)` em try/except OSError) e re-raise. Envolver o todo em try/except OSError logando via `logger.error("Falha ao salvar configuracao: %s", exc)`.

    Os 11 callsites de `salvar_config_automatico()` NAO precisam mudar — continuam chamando o metodo, que agora apenas agenda. CRITICO: garantir que `open("config.json", "w")` (string literal relativa) NAO permaneca em nenhum lugar de main_window.py — o write agora usa `self._config_path`.
  </action>
  <verify>
    <automated>grep -q 'import tempfile' ui/main_window.py && grep -q 'QTimer' ui/main_window.py && grep -q 'self._save_timer.setSingleShot(True)' ui/main_window.py && grep -q 'def _do_save_config' ui/main_window.py && grep -q 'os.replace' ui/main_window.py && grep -q 'self._save_timer.start(500)' ui/main_window.py && ! grep -q 'open("config.json"' ui/main_window.py && ! grep -q "open('config.json'" ui/main_window.py && python -c "import ast; ast.parse(open('ui/main_window.py',encoding='utf-8').read()); print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `import tempfile` e `QTimer` importados em main_window.py
    - `self._save_timer` e `QTimer(self)` com `setSingleShot(True)` conectado a `_do_save_config`
    - `salvar_config_automatico()` apenas chama `self._save_timer.start(500)` (sem write direto, sem _sync inline)
    - Existe `_do_save_config()` que faz `tempfile.mkstemp(dir=...)` + `os.replace(..., str(self._config_path))`
    - A string `open("config.json"` e `open('config.json'` NAO aparecem em main_window.py
    - main_window.py compila (ast.parse)
  </acceptance_criteria>
  <done>salvar_config_automatico() debounça em 500ms via QTimer cancelavel; _do_save_config() escreve atomicamente via tempfile+os.replace no self._config_path; o open() relativo foi eliminado; o arquivo compila.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificar path absoluto (system32) e ausencia de corrupcao/temp files</name>
  <read_first>
    - main.py (apos Task 1)
    - ui/main_window.py (apos Task 2)
    - .planning/phases/01-foundation/01-VALIDATION.md (Manual-Only Verifications — ENG-05 e ENG-06)
  </read_first>
  <action>Pedir ao usuario que rode o app de um CWD diferente (ENG-05) e mova sliders por 10s (ENG-06), confirmando que nenhum config.json e criado no CWD, nenhum .tmp acumula, e o config.json permanece valido. Nenhum codigo e escrito nesta task — e validacao dos artefatos das Tasks 1 e 2.</action>
  <what-built>
    O config agora e lido e escrito por caminho absoluto derivado de `__file__`, e o save debounça 500ms com write atomico (tempfile + os.replace). As provas de ENG-05 (CWD independente) e ENG-06 (sem corrupcao / sem temp files acumulados) exigem rodar o app de um CWD diferente e interagir com a UI — manual.
  </what-built>
  <how-to-verify>
    ENG-05 (path absoluto):
    1. Abra um terminal e mude o CWD para outro diretorio, ex: `cd C:\\Windows\\system32` (ou qualquer pasta != raiz do projeto)
    2. Rode o app pelo caminho absoluto: `python D:\\Documentos\\Codes\\OBS_GestureNode\\main.py`
    3. Confirme que NENHUM `config.json` novo e criado no CWD (ex: nao aparece em system32)
    4. Edite algo na UI e confirme que o save altera o `config.json` na raiz do projeto

    ENG-06 (debounce + atomicidade):
    5. Com o app aberto, mova qualquer slider (hold_time/cooldown) rapidamente por ~10 segundos
    6. Confirme que NAO ficam arquivos `.tmp` acumulados na raiz do projeto
    7. Abra `config.json` e confirme que e JSON valido (sem corrupcao / sem conteudo truncado)
  </how-to-verify>
  <acceptance_criteria>
    - Rodar o app de outro CWD NAO cria config.json no CWD (ENG-05)
    - Editar na UI grava no config.json da raiz do projeto
    - Mover sliders por 10s NAO deixa arquivos .tmp acumulados (ENG-06)
    - config.json permanece JSON valido apos a edicao intensa
  </acceptance_criteria>
  <resume-signal>Digite "approved" se ambos os testes passaram, ou descreva o que ocorreu</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processo do app -> filesystem (config.json) | Escrita de config no disco pode ser interrompida por crash/kill |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | corrupcao de config.json por write nao-atomico interrompido | mitigate | ENG-06: tempfile.mkstemp na mesma particao + os.replace() (atomico no NTFS); processo morto no meio nunca deixa config parcial |
| T-05-02 | Tampering | config.json escrito no CWD errado (ex: system32) | mitigate | ENG-05: Path(__file__).parent resolve caminho absoluto independente do CWD; elimina escrita em diretorio nao intencional |
| T-05-03 | Denial of Service | dezenas de writes por movimento de slider degradam I/O e ampliam janela de corrupcao | mitigate | ENG-06: debounce 500ms via QTimer cancelavel reduz N writes a 1 |
</threat_model>

<verification>
- `grep 'Path(__file__).parent / "config.json"' main.py` e `grep 'os.replace' ui/main_window.py` retornam match
- `grep 'open("config.json"' ui/main_window.py` falha (string ausente)
- App rodado de outro CWD nao cria config.json no CWD (verificado por humano — Task 3)
- Mover sliders 10s nao deixa .tmp acumulados e config.json permanece valido (verificado por humano — Task 3)
</verification>

<success_criteria>
- Roadmap Success Criterion 1: app via atalho carrega config sem criar arquivo em C:\\Windows\\system32 — SATISFEITO (ENG-05)
- Roadmap Success Criterion 2: mover sliders 10s nao gera temp files nem corrompe config.json — SATISFEITO (ENG-06)
- ENG-05 (path absoluto via __file__) e ENG-06 (debounce 500ms + write atomico) cobertos
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

Novos simbolos:
- `main.py`: `CONFIG_PATH` (constante de modulo, Path absoluto via __file__).
- `MainWindow._config_path` (atributo, Path) — caminho absoluto do config.
- `MainWindow._save_timer` (atributo, QTimer cancelavel single-shot) — debounce.
- `MainWindow._do_save_config()` (metodo) — write atomico via tempfile + os.replace.

Simbolos modificados:
- `carregar_config()` em main.py — default agora CONFIG_PATH.
- `MainWindow.__init__()` — aceita parametro `config_path`.
- `MainWindow.salvar_config_automatico()` — agora apenas agenda o debounce (start(500)).
</artifacts_produced>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done
</output>
