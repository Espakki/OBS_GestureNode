---
phase: 08-modos-de-opera-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/gesture_engine.py
  - actions/action_manager.py
autonomous: true
requirements: [MODE-01, MODE-02, MODE-03]
must_haves:
  truths:
    - "Em modo 'teste', nenhuma ação (cena, hotkey, áudio) é executada — bloqueio na ActionManager e no engine (D-02)"
    - "Em modo 'manual' e 'automatico', o engine conecta ao OBS automaticamente ao iniciar (D-04)"
    - "VCam (pyvirtualcam) só é ativada quando modo == 'automatico'"
    - "Ao iniciar a engine em 'teste', o status 'Modo Teste — ações desativadas' é emitido (D-03)"
  artifacts:
    - "engine/gesture_engine.py"
    - "actions/action_manager.py"
  key_links:
    - "GestureEngine._setup() captura self.modo e o repassa ao ActionManager e deriva enable_virtual_camera"
    - "GestureEngine._connect_obs() conecta apenas para 'manual'/'automatico'"
    - "ActionManager.executar() retorna cedo quando self.modo == 'teste'"
---

<objective>
Implementar a matriz de comportamento dos três modos no backend (engine + ações), sem tocar na UI.

Matriz (D-02, D-04):

| Modo       | WebSocket OBS | VCam (pyvirtualcam) | Hotkeys/Áudio |
|------------|---------------|---------------------|---------------|
| teste      | ❌            | ❌                  | ❌            |
| manual     | ✅ auto       | ❌                  | ✅            |
| automatico | ✅ auto       | ✅                  | ✅            |

Purpose: O modo controla exclusivamente a integração WebSocket/VCam e o bloqueio de ações. Esta é a camada de comportamento — a UI (Plan 02/03) apenas seleciona o valor canônico do modo.
Output: `engine/gesture_engine.py` e `actions/action_manager.py` atualizados para ler `config["modo"]` com valores canônicos `"teste"`/`"manual"`/`"automatico"` (D-09) e aplicar a matriz.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-modos-de-opera-o/08-CONTEXT.md

# Fonte de verdade dos valores canônicos de modo: 08-CONTEXT.md §decisions (D-09)
# Comportamento por modo: 08-CONTEXT.md §domain (matriz)
@engine/gesture_engine.py
@actions/action_manager.py
@integrations/obs_connect_thread.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloquear todas as ações da ActionManager em modo "teste"</name>
  <files>actions/action_manager.py</files>
  <read_first>
    - actions/action_manager.py (estado atual: `__init__(self, obs_controller)` na linha 43; `executar(self, tipo, valor=None)` na linha 54)
    - .planning/phases/08-modos-de-opera-o/08-CONTEXT.md (D-02: modo Teste bloqueia OBS, hotkeys E áudio; D-09: valores canônicos)
  </read_first>
  <action>
    Adicionar um parâmetro `modo` ao construtor de `ActionManager`: alterar a assinatura para `def __init__(self, obs_controller, modo="automatico"):` e armazenar `self.modo = str(modo or "automatico").lower()` logo após `self.obs = obs_controller`. O default `"automatico"` garante que um ActionManager sem modo explícito não bloqueie silenciosamente ações (least surprise para qualquer chamador).
    No início de `executar(self, tipo, valor=None)`, antes de qualquer despacho (`trocar_cena`, `iniciar_live`, `parar_live`, `tocar_som`, `atalho`), inserir um guard que implementa D-02: quando `self.modo == "teste"`, registrar via `logger.info` que a ação foi bloqueada pelo modo Teste e retornar imediatamente — nenhuma chamada a `winsound`, `SendInput` ou OBS pode ocorrer. Use o identificador de modo `"teste"` (string canônica, sem acento, D-09).
    Não alterar os métodos privados de hotkey/áudio (`_acionar_atalho`, `_tocar_som`, `_enviar_atalho*`) — o bloqueio é centralizado no ponto de entrada `executar()`.
  </action>
  <verify>
    <automated>python -c "from actions.action_manager import ActionManager; am=ActionManager(None, modo='teste'); assert am.modo=='teste'; am.executar('atalho','Ctrl+Shift+X'); am.executar('tocar_som','x.wav'); am.executar('trocar_cena','Cena'); a=ActionManager(None); assert a.modo=='automatico'; print('actionmanager-teste-block-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `ActionManager.__init__` aceita o parâmetro `modo` com default `"automatico"` e expõe `self.modo` (lowercase).
    - Chamar `executar("atalho", ...)`, `executar("tocar_som", ...)` e `executar("trocar_cena", ...)` com `self.modo == "teste"` retorna sem efeito colateral e sem lançar exceção (smoke acima imprime `actionmanager-teste-block-ok`).
    - `grep -n "self.modo" actions/action_manager.py` mostra o guard dentro de `executar` antes do primeiro `if tipo ==`.
  </acceptance_criteria>
  <done>ActionManager bloqueia 100% das ações quando o modo é "teste" e opera normalmente nos demais modos (D-02).</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar a matriz de modo no GestureEngine (OBS, VCam, bloqueio, status)</name>
  <files>engine/gesture_engine.py</files>
  <read_first>
    - engine/gesture_engine.py (estado atual: `_setup()` linha 179; CameraManager criado em 222-230 com `enable_virtual_camera=camera_cfg.get("enable_virtual_camera")`; `ActionManager(None)` em 237; `_connect_obs()` em 294 com guard `if self.config.get("modo") != "obs": return`; loop `run()` em 320; bloco de submissão de ação em 391-438; emit de status de câmera em 338)
    - .planning/phases/08-modos-de-opera-o/08-CONTEXT.md (matriz; D-02 bloqueio total no Teste; D-03 status passivo; D-04 Manual = auto OBS sem VCam; D-09 valores canônicos)
    - actions/action_manager.py (assinatura nova `ActionManager(obs_controller, modo=...)` da Task 1)
  </read_first>
  <action>
    1. Normalização de modo: no início de `_setup()`, derivar e armazenar o modo canônico. Ler `raw = str(self.config.get("modo", "automatico")).lower()` e mapear valores legados v1.1 → v1.2: `"test"` vira `"teste"`, `"obs"` vira `"automatico"`. Se `raw` não estiver em `{"teste","manual","automatico"}` após o mapeamento, usar `"automatico"` (padrão de fábrica, D-01). Guardar o resultado em `self.modo`. Isto torna o engine robusto mesmo se a migração da UI (Plan 03) não tiver rodado.
    2. VCam por modo: na construção de `CameraManager` (atualmente `enable_virtual_camera=camera_cfg.get("enable_virtual_camera")`), passar `enable_virtual_camera=(self.modo == "automatico")`. O modo passa a ser a única fonte de verdade da ativação da câmera virtual — a VCam só liga no modo automatico (matriz).
    3. ActionManager com modo: alterar `self.actions = ActionManager(None)` para `self.actions = ActionManager(None, modo=self.modo)`, propagando o bloqueio do Teste (Task 1, D-02).
    4. Conexão OBS por modo: em `_connect_obs()`, substituir o guard `if self.config.get("modo") != "obs": return` por um guard que conecta apenas nos modos com WebSocket: retornar cedo quando `self.modo not in ("manual", "automatico")`. Manter o resto do método (OBSController, handshake, status, `_classificar_erro`) inalterado — Manual e Automático compartilham exatamente a mesma conexão automática (D-04, D-06).
    5. Status passivo do Teste: em `run()`, logo após o emit `"Câmera iniciada"` (linha 338), emitir o status de bloqueio quando aplicável: se `self.modo == "teste"`, chamar `self.status_changed.emit("Modo Teste — ações desativadas")` (D-03 — mensagem passiva e persistente; não é modal).
    6. Bloqueio de ações no loop: no bloco que submete ações ao executor (linha ~395, atualmente `if self.actions and gesture_cfg:`), adicionar a condição de modo para que NENHUMA ação seja submetida e NENHUM status "acionado" seja emitido em Teste — alterar a condição para `if self.actions and gesture_cfg and self.modo != "teste":`. Isto mantém o status "Modo Teste — ações desativadas" como estado de repouso e satisfaz D-02 no nível do engine (defesa em profundidade com a Task 1). A detecção e o preview do gesto continuam funcionando normalmente no Teste (MODE-01).
  </action>
  <verify>
    <automated>python -c "import ast; src=open('engine/gesture_engine.py',encoding='utf-8').read(); ast.parse(src); assert 'self.modo' in src; assert '\"manual\"' in src and '\"automatico\"' in src; assert 'Modo Teste' in src; assert 'self.modo != \"teste\"' in src or \"self.modo != 'teste'\" in src; print('engine-src-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `engine/gesture_engine.py` compila (ast.parse) e define `self.modo` em `_setup()` com normalização de `"test"`→`"teste"` e `"obs"`→`"automatico"`.
    - `CameraManager` recebe `enable_virtual_camera` derivado de `self.modo == "automatico"` (fonte: leitura do trecho de construção do CameraManager).
    - `ActionManager` é instanciado com `modo=self.modo`.
    - `_connect_obs()` retorna cedo quando `self.modo` não é `"manual"` nem `"automatico"`; conecta nos dois (fonte: trecho do guard).
    - O loop não submete ações nem emite status "acionado" quando `self.modo == "teste"` (condição `self.modo != "teste"` no guard de submissão).
    - O status `"Modo Teste — ações desativadas"` é emitido no startup quando `self.modo == "teste"` (D-03).
  </acceptance_criteria>
  <done>O engine aplica integralmente a matriz: OBS conecta em manual/automatico, VCam só em automatico, ações bloqueadas em teste com status passivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário/disco → engine | `config["modo"]` lido do config.json (arquivo local editável pelo usuário) cruza para o engine |
| engine → OBS WebSocket | conexão localhost iniciada em manual/automatico |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `config["modo"]` adulterado/corrompido | low | mitigate | `_setup()` normaliza para `{teste,manual,automatico}`; valor desconhecido cai em `"automatico"` (sem crash, sem estado indefinido) |
| T-08-02 | Information Disclosure | host/porta/senha OBS no config | low | accept | Conexão é localhost por padrão e controlada pelo usuário; falhas tratadas por `_classificar_erro` (Phase 3) |
| T-08-03 | Denial of Service | `pyvirtualcam.Camera()` falha em automatico sem driver instalado | low | accept | Robustez de VCam é escopo explícito da Phase 13; D-01 aceita automatico como padrão pois o engine já tolera OBS ausente |

Sem instalação de pacotes nesta fase — sem necessidade de auditoria de legitimidade de pacote. Nenhuma ameaça atinge o limiar de bloqueio (high).
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('engine/gesture_engine.py',encoding='utf-8').read()); ast.parse(open('actions/action_manager.py',encoding='utf-8').read()); print('parse-ok')"`
- Smoke da Task 1 imprime `actionmanager-teste-block-ok`.
- Smoke da Task 2 imprime `engine-src-ok`.
</verification>

<success_criteria>
- Modo teste: ActionManager e engine não executam nenhuma ação; status passivo emitido (MODE-01, D-02, D-03).
- Modo manual: engine auto-conecta OBS, VCam desligada (MODE-02, D-04).
- Modo automatico: engine auto-conecta OBS, VCam ligada (MODE-03).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- `ActionManager.__init__(self, obs_controller, modo="automatico")` — novo parâmetro `modo`
- `ActionManager.modo` — novo atributo de instância (string canônica lowercase)
- guard em `ActionManager.executar()` — retorno antecipado quando `modo == "teste"`
- `GestureEngine.modo` — novo atributo de instância (modo canônico normalizado)
- Normalização de modo no engine: `"test"`→`"teste"`, `"obs"`→`"automatico"`, desconhecido→`"automatico"`
- String de status: `"Modo Teste — ações desativadas"`
- Derivação `enable_virtual_camera = (self.modo == "automatico")` no engine
</artifacts_produced>

<output>
Create `.planning/phases/08-modos-de-opera-o/08-01-SUMMARY.md` when done
</output>
