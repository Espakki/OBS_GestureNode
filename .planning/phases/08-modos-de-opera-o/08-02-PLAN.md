---
phase: 08-modos-de-opera-o
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/tabs/geral_tab.py
autonomous: true
requirements: [MODE-01, MODE-02, MODE-03]
must_haves:
  truths:
    - "A aba Geral exibe três botões checkáveis exclusivos: Teste, Manual, Automático (D-07, D-08)"
    - "GeralTab.set_mode marca o botão correto para cada valor canônico 'teste'/'manual'/'automatico' (D-09)"
    - "Tooltips e mode_help_label descrevem os três modos de forma orientada ao streamer (D-08)"
  artifacts:
    - "ui/tabs/geral_tab.py"
  key_links:
    - "mode_group (QButtonGroup exclusivo) contém os três botões"
    - "mode_auto_button / mode_manual_button / mode_test_button expostos como atributos para o main_window"
---

<objective>
Expandir a linha de seleção de modo na aba Geral de 2 para 3 botões (Teste | Manual | Automático), seguindo o padrão existente de `QPushButton` checkável em `QButtonGroup` (D-07). Renomear o botão "OBS" para "Automático" e adicionar o botão "Manual".

Purpose: Esta é a superfície de UI dos três modos. Não contém lógica de comportamento — apenas widgets, rótulos, tooltips e o método `set_mode`. A lógica de wiring/persistência fica no Plan 03.
Output: `ui/tabs/geral_tab.py` com três botões de modo, atributos renomeados/adicionados e `set_mode` para três modos.

CONTRATO DE NOMES (consumido pelo Plan 03): os atributos expostos serão `mode_test_button`, `mode_manual_button`, `mode_auto_button`. O atributo antigo `mode_obs_button` deixa de existir.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-modos-de-opera-o/08-CONTEXT.md

@ui/tabs/geral_tab.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar o botão Manual e renomear OBS → Automático (widgets, tooltips, help)</name>
  <files>ui/tabs/geral_tab.py</files>
  <read_first>
    - ui/tabs/geral_tab.py (estado atual: `mode_group` linha 42; `mode_test_button`/`mode_obs_button` linhas 48-49; loop de configuração dos botões 50-56; tooltips 59-64; `mode_help_label` 66-72)
    - .planning/phases/08-modos-de-opera-o/08-CONTEXT.md (D-07 três QPushButton checkáveis em linha; D-08 labels Teste | Manual | Automático; §specifics texto do help orientado ao streamer)
  </read_first>
  <action>
    Renomear o atributo `self.mode_obs_button` para `self.mode_auto_button` e alterar seu texto de `"OBS"` para `"Automático"`. Adicionar um novo botão `self.mode_manual_button = QPushButton("Manual")` posicionado ENTRE Teste e Automático, de modo que a ordem visual da esquerda para a direita seja: Teste, Manual, Automático (D-08).
    Incluir os três botões no loop de configuração (que aplica `setObjectName("optionToggle")`, `setCheckable(True)`, `setMinimumWidth(92)`, `setMinimumHeight(38)`, `addWidget` e `mode_group.addButton`) — ajustar a tupla iterada para `(self.mode_test_button, self.mode_manual_button, self.mode_auto_button)`. O `mode_group` permanece exclusivo (`setExclusive(True)`), garantindo seleção única (D-07).
    Atualizar os tooltips para os três modos, orientados ao streamer:
    - Teste: calibrar gestos, câmera e ações sem executar nada — nenhum comando ao OBS, hotkey ou áudio.
    - Manual: conecta automaticamente ao OBS e executa hotkeys/áudio, mas mantém a câmera virtual desligada (para quem tem conflito de driver de VCam).
    - Automático: gerencia conexão ao OBS e câmera virtual automaticamente ao iniciar.
    Atualizar `self.mode_help_label` para descrever os três modos em três linhas, no mesmo tom do texto existente (ex.: "Automático: câmera virtual e OBS gerenciados automaticamente." conforme §specifics do CONTEXT). Manter `setObjectName("muted")` e `setWordWrap(True)`.
  </action>
  <verify>
    <automated>QT_QPA_PLATFORM=offscreen python -c "import sys; from PySide6.QtWidgets import QApplication; app=QApplication(sys.argv); from ui.tabs.geral_tab import GeralTab; t=GeralTab(); assert hasattr(t,'mode_manual_button'); assert hasattr(t,'mode_auto_button'); assert not hasattr(t,'mode_obs_button'); assert t.mode_test_button.text()=='Teste'; assert t.mode_manual_button.text()=='Manual'; assert t.mode_auto_button.text()=='Automático'; assert t.mode_group.exclusive(); assert len(t.mode_group.buttons())==3; print('geral-widgets-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `GeralTab` expõe `mode_test_button`, `mode_manual_button`, `mode_auto_button` e NÃO expõe mais `mode_obs_button` (asserts do smoke).
    - Textos exatos dos botões: `"Teste"`, `"Manual"`, `"Automático"`.
    - `mode_group` é exclusivo e contém exatamente 3 botões.
    - `mode_help_label` contém uma descrição para cada um dos três modos (verificável lendo o texto do label).
    - Cada botão tem tooltip não vazio descrevendo seu comportamento.
  </acceptance_criteria>
  <done>Três botões de modo presentes, exclusivos, com rótulos Teste/Manual/Automático, tooltips e help atualizados (D-07, D-08).</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar set_mode para os três valores canônicos</name>
  <files>ui/tabs/geral_tab.py</files>
  <read_first>
    - ui/tabs/geral_tab.py (estado atual: `set_mode(self, modo)` linhas 148-152 — só trata `"obs"` vs. fallback)
    - .planning/phases/08-modos-de-opera-o/08-CONTEXT.md (D-09 valores canônicos 'teste'/'manual'/'automatico')
  </read_first>
  <action>
    Reescrever `set_mode(self, modo)` para mapear os três valores canônicos (D-09) ao botão correspondente, normalizando com `str(modo).lower()`:
    - `"automatico"` marca `self.mode_auto_button`;
    - `"manual"` marca `self.mode_manual_button`;
    - qualquer outro valor (incluindo `"teste"` e valores legados/desconhecidos) marca `self.mode_test_button` como fallback seguro (sandbox).
    Usar `setChecked(True)` no botão alvo. Como o `mode_group` é exclusivo, marcar um botão desmarca os demais automaticamente.
  </action>
  <verify>
    <automated>QT_QPA_PLATFORM=offscreen python -c "import sys; from PySide6.QtWidgets import QApplication; app=QApplication(sys.argv); from ui.tabs.geral_tab import GeralTab; t=GeralTab(); t.set_mode('automatico'); assert t.mode_auto_button.isChecked(); t.set_mode('manual'); assert t.mode_manual_button.isChecked(); t.set_mode('teste'); assert t.mode_test_button.isChecked(); t.set_mode('valor_invalido'); assert t.mode_test_button.isChecked(); print('set-mode-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `set_mode("automatico")` deixa apenas `mode_auto_button` marcado.
    - `set_mode("manual")` deixa apenas `mode_manual_button` marcado.
    - `set_mode("teste")` e qualquer valor desconhecido deixam apenas `mode_test_button` marcado (fallback).
    - Smoke imprime `set-mode-ok`.
  </acceptance_criteria>
  <done>set_mode restaura corretamente o botão para os três modos e usa Teste como fallback seguro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config (valor de modo) → UI | `set_mode` recebe o valor persistido e seleciona o botão; valor inesperado não pode quebrar a UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Tampering | valor de modo inesperado passado a `set_mode` | low | mitigate | Fallback explícito para `mode_test_button` em valores não reconhecidos (sandbox seguro) |

Sem instalação de pacotes. UI local sem exposição de rede. Nenhuma ameaça atinge o limiar de bloqueio.
</threat_model>

<verification>
- `QT_QPA_PLATFORM=offscreen python -c "..."` (smokes das Tasks 1 e 2) imprimem `geral-widgets-ok` e `set-mode-ok`.
- `python -c "import ast; ast.parse(open('ui/tabs/geral_tab.py',encoding='utf-8').read()); print('parse-ok')"`
</verification>

<success_criteria>
- Aba Geral mostra três botões de modo exclusivos com os rótulos corretos (MODE-01, MODE-02, MODE-03).
- set_mode restaura o botão correto a partir do valor canônico (suporta MODE-04 no Plan 03).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- `GeralTab.mode_manual_button` — novo QPushButton ("Manual")
- `GeralTab.mode_auto_button` — renomeado de `mode_obs_button` (texto "Automático")
- Remoção de `GeralTab.mode_obs_button` (atributo deixa de existir)
- `GeralTab.set_mode()` — reescrito para três modos canônicos com fallback Teste
- Tooltips e `mode_help_label` atualizados para descrever os três modos
</artifacts_produced>

<output>
Create `.planning/phases/08-modos-de-opera-o/08-02-SUMMARY.md` when done
</output>
