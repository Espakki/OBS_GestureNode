---
phase: 08-modos-de-opera-o
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - ui/main_window.py
  - config.json
autonomous: true
requirements: [MODE-01, MODE-02, MODE-03, MODE-04]
must_haves:
  truths:
    - "config.json migra modo legado 'test'→'teste' e 'obs'→'automatico' sem perda de bindings (MODE-04, D-09)"
    - "Padrão de fábrica (modo ausente) é 'automatico' (D-01)"
    - "A seleção de modo persiste no config.json e é restaurada ao reabrir o app (MODE-04)"
    - "on_modo_changed grava enable_virtual_camera = (modo == 'automatico') (D-04)"
    - "Health e validação de OBS aplicam-se a 'manual' e 'automatico'; 'teste' aparece como desativado (D-03)"
  artifacts:
    - "ui/main_window.py"
    - "config.json"
  key_links:
    - "mode_test_button/mode_manual_button/mode_auto_button .toggled → on_modo_changed(valor canônico)"
    - "_init_config_schema normaliza/migra config['modo'] para um dos três valores canônicos"
    - "_load_ui_from_config chama geral_tab.set_mode(config['modo'])"
---

<objective>
Integrar os três modos no `main_window`: migração silenciosa do config v1.1, wiring dos três botões, persistência, mapeamento de VCam por modo, e atualização das verificações de health/validação que hoje testam `modo == "obs"`.

Purpose: Fecha a persistência (MODE-04) e conecta a UI (Plan 02) ao comportamento (Plan 01). Garante que configs v1.1 (`"modo": "test"` / `"obs"`) carreguem sem exceção e com o modo correto.
Output: `ui/main_window.py` com migração + wiring + health para 3 modos; `config.json` com `"modo"` migrado.

DEPENDÊNCIA: usa os atributos `mode_manual_button` e `mode_auto_button` criados no Plan 02. Usa os valores canônicos `"teste"`/`"manual"`/`"automatico"` lidos pelo engine do Plan 01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-modos-de-opera-o/08-CONTEXT.md

@ui/main_window.py
@ui/tabs/geral_tab.py
@config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migração silenciosa do modo e padrão de fábrica "automatico"</name>
  <files>ui/main_window.py</files>
  <read_first>
    - ui/main_window.py (estado atual: `_init_config_schema` linha 114; `self.config.setdefault("modo", "test")` linha 115)
    - config.json (campo `"modo": "test"` na linha 2)
    - .planning/phases/08-modos-de-opera-o/08-CONTEXT.md (D-01 padrão automatico substitui MODE-04 default; D-09 valores canônicos; §code_context migração v1.1→v1.2: test→teste, obs→automatico)
  </read_first>
  <action>
    Substituir a linha `self.config.setdefault("modo", "test")` por uma migração determinística que coage `config["modo"]` para um dos três valores canônicos:
    - Definir os conjuntos: valores válidos `{"teste", "manual", "automatico"}` e mapa legado `{"test": "teste", "obs": "automatico"}`.
    - Ler `raw = self.config.get("modo")`. Se `raw` estiver no mapa legado, usar o valor mapeado. Senão, se `raw` já estiver no conjunto válido, mantê-lo. Caso contrário (ausente, `None` ou desconhecido), usar `"automatico"` — o padrão de fábrica de D-01.
    - Gravar o resultado de volta em `self.config["modo"]`.
    Esta migração é silenciosa e não toca em `gestures`/`bindings` (sem perda de configuração existente, MODE-04). Como `_init_config_schema` roda antes da criação do engine, o engine (Plan 01) já recebe um valor canônico.
  </action>
  <verify>
    <automated>python -c "import ast; t=ast.parse(open('ui/main_window.py',encoding='utf-8').read()); src=open('ui/main_window.py',encoding='utf-8').read(); assert 'setdefault(\"modo\", \"test\")' not in src; assert '\"automatico\"' in src and '\"teste\"' in src; assert ('\"test\"' in src and '\"obs\"' in src); print('migracao-src-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - A chamada antiga `setdefault("modo", "test")` não existe mais em `ui/main_window.py`.
    - O código contém o mapa legado (`"test"`→`"teste"`, `"obs"`→`"automatico"`) e o default `"automatico"` para modo ausente/desconhecido (fonte: trecho de migração em `_init_config_schema`).
    - Comportamento (rastreável por leitura): config sem `"modo"` resulta em `"automatico"`; `"test"` resulta em `"teste"`; `"obs"` resulta em `"automatico"`; `"manual"` permanece `"manual"`.
    - Smoke imprime `migracao-src-ok`.
  </acceptance_criteria>
  <done>Configs v1.1 carregam migrados sem exceção; modo ausente assume "automatico" (MODE-04, D-01, D-09).</done>
</task>

<task type="auto">
  <name>Task 2: Wiring dos três botões e mapeamento de VCam por modo</name>
  <files>ui/main_window.py</files>
  <read_first>
    - ui/main_window.py (estado atual: aliases de widget 421-422 `self.mode_test_button`/`self.mode_obs_button = self.geral_tab.mode_obs_button`; wiring `.toggled` 455-456; `_load_ui_from_config` chama `geral_tab.set_mode` na linha 765; `on_modo_changed` 936-941 com `enable_virtual_camera = modo == "obs"`)
    - ui/tabs/geral_tab.py (atributos novos do Plan 02: `mode_manual_button`, `mode_auto_button`)
    - .planning/phases/08-modos-de-opera-o/08-CONTEXT.md (D-04 VCam só no automatico; D-06 botão "Conectar OBS" continua funcional no Manual; D-09 valores canônicos)
  </read_first>
  <action>
    1. Aliases de widget (linhas 421-422): substituir `self.mode_obs_button = self.geral_tab.mode_obs_button` por `self.mode_auto_button = self.geral_tab.mode_auto_button` e adicionar `self.mode_manual_button = self.geral_tab.mode_manual_button`. Manter `self.mode_test_button = self.geral_tab.mode_test_button`.
    2. Wiring de sinais (linhas 455-456): conectar os três botões ao `on_modo_changed` com os valores canônicos (D-09):
       - `mode_test_button.toggled` → `on_modo_changed("teste")` quando checado;
       - `mode_manual_button.toggled` → `on_modo_changed("manual")` quando checado;
       - `mode_auto_button.toggled` → `on_modo_changed("automatico")` quando checado.
       Seguir o padrão lambda existente (`lambda checked: self.on_modo_changed(<valor>) if checked else None`).
    3. Restauração na carga (linha 765): alterar o default de `self.geral_tab.set_mode(self.config.get("modo", "test"))` para `self.config.get("modo", "automatico")` (D-01). Como a Task 1 já normalizou o config, o valor passado será sempre canônico.
    4. `on_modo_changed(self, modo)` (936-941): manter `self.config["modo"] = modo`; alterar a linha de VCam de `self.config["camera"]["enable_virtual_camera"] = modo == "obs"` para `modo == "automatico"` (D-04 — VCam só no automatico). Manter as chamadas `self._refresh_health_panels()` e `self.salvar_config_automatico()`. Não adicionar restart de engine — a matriz de comportamento é aplicada no próximo start do engine (consistente com a arquitetura existente). O botão "Conectar OBS" da aba OBS permanece inalterado e funcional em Manual (D-06).
  </action>
  <verify>
    <automated>python -c "src=open('ui/main_window.py',encoding='utf-8').read(); assert 'mode_obs_button' not in src; assert 'mode_auto_button' in src and 'mode_manual_button' in src; assert 'on_modo_changed(\"teste\")' in src and 'on_modo_changed(\"manual\")' in src and 'on_modo_changed(\"automatico\")' in src; assert 'enable_virtual_camera\"] = modo == \"automatico\"' in src or \"enable_virtual_camera'] = modo == 'automatico'\" in src; import ast; ast.parse(src); print('wiring-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Nenhuma referência a `mode_obs_button` permanece em `ui/main_window.py`.
    - Os três botões estão conectados a `on_modo_changed` com os valores `"teste"`, `"manual"`, `"automatico"` respectivamente.
    - `_load_ui_from_config` restaura o modo via `set_mode(self.config.get("modo", "automatico"))`.
    - `on_modo_changed` grava `enable_virtual_camera = (modo == "automatico")` (D-04).
    - Smoke imprime `wiring-ok`.
  </acceptance_criteria>
  <done>Os três botões controlam e persistem o modo; VCam é habilitada apenas no automatico (MODE-01/02/03, D-04).</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar health e validação para os três modos</name>
  <files>ui/main_window.py</files>
  <read_first>
    - ui/main_window.py (estado atual: `_validar_config_execucao` 1126-1140 com `if modo == "obs":` na 1135 e default `"test"` na 1130; `_refresh_health_panels` 1292-1313 com `modo = self.config.get("modo", "test")` na 1293, `if modo == "obs":` na 1304 e label "Desativado (modo test)" na 1313)
    - .planning/phases/08-modos-de-opera-o/08-CONTEXT.md (matriz: manual e automatico têm OBS; teste não; D-03 indicação passiva)
  </read_first>
  <action>
    1. `_validar_config_execucao` (linha 1130 e 1135): alterar o default de `self.config.get("modo", "test")` para `"automatico"`. Alterar a condição `if modo == "obs":` (que valida host/porta do OBS) para `if modo in ("manual", "automatico"):`, pois ambos auto-conectam ao OBS e portanto precisam de host/porta válidos.
    2. `_refresh_health_panels` (linha 1293, 1304, 1313): alterar o default de `self.config.get("modo", "test")` para `"automatico"`. Alterar a condição `if modo == "obs":` para `if modo in ("manual", "automatico"):`. No ramo `else` (modo teste), alterar o texto do health label de `"Desativado (modo test)"` para `"Desativado (modo Teste)"` — refletindo que em Teste o OBS está desativado (D-03).
  </action>
  <verify>
    <automated>python -c "import ast; src=open('ui/main_window.py',encoding='utf-8').read(); ast.parse(src); assert src.count('get(\"modo\", \"test\")')==0; assert 'modo in (\"manual\", \"automatico\")' in src; assert 'modo == \"obs\"' not in src; assert 'Desativado (modo Teste)' in src; print('health-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Não há mais `self.config.get("modo", "test")` em `ui/main_window.py` (defaults agora `"automatico"`).
    - Não há mais condição `modo == "obs"` em `ui/main_window.py`.
    - Validação de host/porta do OBS ocorre para `manual` e `automatico` (`modo in ("manual", "automatico")`).
    - O health label do OBS em modo teste mostra `"Desativado (modo Teste)"`.
    - Smoke imprime `health-ok`.
  </acceptance_criteria>
  <done>Health e validação refletem a matriz dos três modos; OBS validado em manual/automatico, desativado em teste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disco → main_window | config.json (incluindo `"modo"` v1.1) é lido e migrado na inicialização |
| usuário → config (persistência) | seleção de modo na UI é gravada via write atômico com debounce (Phase 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05 | Tampering | `config["modo"]` legado/corrompido na carga | low | mitigate | `_init_config_schema` migra/coage para `{teste,manual,automatico}`; default `automatico` |
| T-08-06 | Repudiation | perda de bindings ao migrar config v1.1 | low | mitigate | Migração toca apenas o campo `modo`; `gestures`/`bindings` preservados; write atômico (Phase 1) impede corrupção |

Sem instalação de pacotes. Persistência local via write atômico já existente. Nenhuma ameaça atinge o limiar de bloqueio.
</threat_model>

<verification>
- Smokes das Tasks 1-3 imprimem `migracao-src-ok`, `wiring-ok`, `health-ok`.
- `python -c "import ast; ast.parse(open('ui/main_window.py',encoding='utf-8').read()); print('parse-ok')"`
- Migração de config.json verificada manualmente no checkpoint do Plan 04 (reabrir o app preserva o modo).
</verification>

<success_criteria>
- Config v1.1 carrega migrado, sem exceção e sem perda de bindings (MODE-04).
- Modo ausente assume automatico (D-01).
- Seleção dos três botões persiste no config.json (MODE-04).
- Health/validação corretos por modo (MODE-01/02/03).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- `MainWindow.mode_manual_button`, `MainWindow.mode_auto_button` — aliases novos (alias `mode_obs_button` removido)
- Migração de modo em `_init_config_schema`: `"test"`→`"teste"`, `"obs"`→`"automatico"`, ausente/desconhecido→`"automatico"`
- `on_modo_changed` grava `enable_virtual_camera = (modo == "automatico")`
- Wiring `.toggled` dos três botões para `on_modo_changed("teste"|"manual"|"automatico")`
- Health label OBS em teste: `"Desativado (modo Teste)"`
- `config.json` com `"modo"` migrado para valor canônico
</artifacts_produced>

<output>
Create `.planning/phases/08-modos-de-opera-o/08-03-SUMMARY.md` when done
</output>
