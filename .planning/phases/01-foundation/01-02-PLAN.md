---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config.json
autonomous: true
requirements: [ENG-05, ENG-06]
must_haves:
  truths:
    - "config.json no repo nao contem nenhum path absoluto do developer"
    - "config.json no repo nao contem o placeholder 'Pressione as teclas...'"
    - "Todos os gestos tem bindings vazios (sem acao configurada)"
    - "camera.index e 0 (camera padrao), nao 1"
    - "config.json e JSON valido"
  artifacts:
    - path: "config.json"
      provides: "Template de config limpo representando install fresh"
      contains: '"index": 0'
  key_links:
    - from: "config.json"
      to: "main.py carregar_config()"
      via: "json.load() le o template no startup"
      pattern: '"modo":\\s*"test"'
---

<objective>
Substituir o `config.json` versionado por um template limpo que representa o estado de "app recem instalado" (D-01, D-02). Hoje o arquivo carrega estado de trabalho do developer: um path absoluto inexistente (`D:/Documentos/Codes/gesto_camera/faz-o-l.wav`), um placeholder de hotkey invalido (`"Pressione as teclas..."`) que seria enviado literalmente como combo de teclas, `camera.index: 1` (especifico da maquina do dev), e bindings pre-preenchidos com cenas/hotkeys.

Purpose: ENG (cleanup de runtime state, suporte a ENG-05/ENG-06) — qualquer clone do repo hoje herda configuracao quebrada. Um template limpo garante que cada usuario parte de um config fresh consistente com o comportamento do .exe. Sem esta limpeza, os fixes de path (ENG-05) e save (ENG-06) operariam sobre dados corrompidos por padrao.
Output: `config.json` limpo — mesma estrutura/chaves, todos os gestos com bindings vazios e desabilitados para acao, camera padrao, sem dados do developer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever config.json como template limpo de install fresh</name>
  <read_first>
    - config.json (arquivo completo — 220 linhas atuais; preservar a estrutura de chaves)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisoes D-01, D-02 e secao "Specific Ideas")
    - .planning/phases/01-foundation/01-RESEARCH.md (secao "Common Pitfalls > Pitfall 5" — campos a limpar)
  </read_first>
  <action>
    Reescrever `config.json` mantendo EXATAMENTE a mesma estrutura de chaves do arquivo atual (mesmos blocos `modo`, `camera`, `obs`, `gestures.bindings`, `gestures.scene_map`, `features`, e os 14 gestos como chaves de display em portugues), mas com os seguintes valores de "fresh install":

    1. `modo`: manter `"test"`.
    2. `camera.index`: trocar de `1` para `0` (camera padrao da maquina, nao a camera 1 do developer).
    3. `camera.device_name`: trocar `"Câmera 1"` para `""` (vazio — sera detectado em runtime).
    4. Manter `camera.width=640`, `height=480`, `fps=30`, `enable_virtual_camera=false`, `virtual_camera_device=null`, `show_preview=false`, `show_skeleton=true`.
    5. `obs.host`: trocar `"192.168.1.3"` (IP da rede do dev) para `"localhost"`. Manter `port=4455`, `password=""`.
    6. Para CADA gesto em `gestures.bindings`, definir bindings vazios e sem acao (D-02): `"scene": ""`, `"sound_file": ""`, `"hotkey": ""`, `"use_scene": false`, `"use_sound": false`, `"use_hotkey": false`, `"play_sound": false`. Manter `"enabled"` e os tempos `hold_time`/`cooldown` por gesto como estao (sao defaults de comportamento, nao acoes).
    7. CRITICO: remover o path absoluto `"D:/Documentos/Codes/gesto_camera/faz-o-l.wav"` (gesto "V") — substituir por `""`.
    8. CRITICO: remover o placeholder `"Pressione as teclas..."` (gesto "Escoteiro" hotkey) — substituir por `""`. E `use_hotkey` do "Escoteiro" -> `false`, `scene` "es" -> `""`.
    9. `gestures.scene_map`: esvaziar para `{}` (nenhuma cena pre-mapeada — bindings vazios nao geram scene_map).
    10. Manter os campos de estabilizacao: `default_hold_time=2.0`, `default_cooldown=2.0`, `enable_stability_check=true`, `motion_pixel_threshold=4`, `stability_min_frames=3`, `check_velocity_trend=true`, e a lista `active_gestures` como esta.
    11. `features`: manter `use_sounds=false`, `use_hotkeys=false`, `use_scene_switch=true`.

    Usar indentacao de 4 espacos e `ensure_ascii` no estilo atual (caracteres unicode escapados como `â` sao aceitaveis pois e como o app salva). Garantir que o resultado e JSON sintaticamente valido.
  </action>
  <verify>
    <automated>python -c "import json; json.load(open('config.json', encoding='utf-8'))" && ! grep -q 'gesto_camera' config.json && ! grep -q 'Pressione as teclas' config.json && ! grep -q '192.168.1.3' config.json && grep -q '\"index\": 0' config.json && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `json.load(open('config.json'))` nao lanca excecao (JSON valido)
    - A string `gesto_camera` NAO aparece no arquivo (path absoluto removido)
    - A string `Pressione as teclas` NAO aparece no arquivo (placeholder removido)
    - A string `192.168.1.3` NAO aparece no arquivo (IP do dev removido)
    - `camera.index` e `0`
    - Para todo gesto em `gestures.bindings`: `use_scene`, `use_sound`, `use_hotkey` sao `false` e `scene`, `sound_file`, `hotkey` sao `""`
    - `gestures.scene_map` e `{}`
  </acceptance_criteria>
  <done>config.json e um template limpo de install fresh: JSON valido, camera.index=0, todos os gestos com bindings vazios/sem acao, scene_map vazio, e zero dados do developer (path absoluto, placeholder de hotkey, IP de rede removidos).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json (versionado) -> clone do usuario | Estado commitado e herdado por todo clone do repositorio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | config.json com path absoluto do dev | mitigate | D-01: template limpo sem hardcode de paths; remover `D:/.../faz-o-l.wav` e substituir por `""` |
| T-02-02 | Information Disclosure | config.json com IP de rede do dev (192.168.1.3) | mitigate | Substituir host OBS por `localhost`; nao vazar topologia de rede do developer no repo publico |
| T-02-03 | Tampering | placeholder de hotkey "Pressione as teclas..." enviado como combo literal | mitigate | Substituir por `""` e `use_hotkey=false`; impede injecao de teclas invalidas no startup |
</threat_model>

<verification>
- `python -c "import json; json.load(open('config.json', encoding='utf-8'))"` sai com codigo 0
- `grep -q 'gesto_camera' config.json` falha (string ausente)
- `grep -q 'Pressione as teclas' config.json` falha (string ausente)
- `grep -q '"index": 0' config.json` sucesso
</verification>

<success_criteria>
- Suporta Roadmap Success Criterion 1 (config carrega corretamente — template valido) e remove dados quebrados que comprometeriam a verificacao de ENG-05/ENG-06
- D-01 (template limpo commitado) e D-02 (bindings vazios) satisfeitos
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

Nenhum novo simbolo de codigo. Apenas modificacao de arquivo de dados:
- `config.json` — reescrito como template limpo de install fresh (camera.index=0, bindings vazios, scene_map vazio, sem dados do developer).
</artifacts_produced>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
