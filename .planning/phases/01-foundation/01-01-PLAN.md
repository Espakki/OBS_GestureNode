---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: false
requirements: [DEP-01, DEP-02]
must_haves:
  truths:
    - "pip install -r requirements.txt instala todas as dependencias sem erro em venv limpo"
    - "pygrabber esta presente no requirements.txt com versao fixada"
    - "Toda dependencia (exceto comentarios) esta pinada com =="
  artifacts:
    - path: "requirements.txt"
      provides: "Lista de dependencias com versoes fixadas"
      contains: "pygrabber==0.2"
  key_links:
    - from: "requirements.txt"
      to: "ambiente venv"
      via: "pip install -r requirements.txt"
      pattern: "pygrabber==0\\.2"
---

<objective>
Reparar o `requirements.txt` quebrado: pinar todas as 6 dependencias com `==` nas versoes verificadas para Python 3.10.11, adicionar `pygrabber==0.2` (atualmente ausente, causando install quebrado), e remover a linha sintaticamente invalida `python version == 3.10.11` substituindo por comentario.

Purpose: ENG-DEP — um `pip install -r requirements.txt` em ambiente limpo hoje falha ou instala versoes imprevisiveis porque nada esta pinado e `pygrabber` (importado em `ui/main_window.py`) nem aparece no arquivo. Esta correcao garante installs reproduziveis (DEP-01, DEP-02) — fundacao para todas as outras fases.
Output: `requirements.txt` reparado, com 7 dependencias pinadas e comentario de versao Python.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@requirements.txt
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verificar legitimidade do pacote pygrabber antes de adicionar</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (secao "Package Legitimacy Audit")
  </read_first>
  <action>Gate de legitimidade bloqueante: pausar e pedir confirmacao humana de que `pygrabber==0.2` e legitimo via pypi.org ANTES de escrever no requirements.txt. Nenhum codigo e escrito nesta task.</action>
  <what-built>
    Nenhum codigo ainda — este e um gate de legitimidade obrigatorio. `pygrabber` foi marcado `[ASSUMED]`/`SUS` pelo seam de auditoria (motivo: `unknown-downloads`, limitacao de API do PyPI, nao risco real). O RESEARCH.md (D-05) propoe `pygrabber==0.2` como unica versao estavel disponivel. Antes de escrever no `requirements.txt`, um humano deve confirmar a legitimidade do pacote.
  </what-built>
  <how-to-verify>
    1. Abra https://pypi.org/project/pygrabber/ no navegador
    2. Confirme que a versao `0.2` existe e e a mais recente publicada
    3. Confirme que o repositorio fonte e github.com/andreaschiavinato/python_grabber
    4. Confirme que o pacote e usado para enumeracao de dispositivos DirectShow no Windows (consistente com o import em ui/main_window.py linha 37)
  </how-to-verify>
  <acceptance_criteria>
    - Humano confirmou via pypi.org/project/pygrabber que `pygrabber==0.2` e legitimo
    - Aprovacao registrada antes de qualquer write no requirements.txt
  </acceptance_criteria>
  <resume-signal>Digite "approved" para confirmar pygrabber==0.2 ou descreva o problema encontrado</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pinar todas as dependencias e adicionar pygrabber no requirements.txt</name>
  <read_first>
    - requirements.txt (arquivo completo — 7 linhas atuais)
    - .planning/phases/01-foundation/01-RESEARCH.md (secao "Standard Stack" e "Code Examples > Exemplo 1: requirements.txt final")
    - .planning/phases/01-foundation/01-CONTEXT.md (decisoes D-03, D-04, D-05, D-06)
  </read_first>
  <action>
    Substituir o conteudo COMPLETO de `requirements.txt` pelo conjunto pinado abaixo, nesta ordem exata. A primeira linha e um comentario de versao Python (substitui a linha invalida `python version == 3.10.11`, que o pip ignora silenciosamente — D-06). `mediapipe==0.10.14` ja estava pinado e DEVE permanecer exatamente assim (D-04, constraint critico do CLAUDE.md — nao alterar). As demais versoes vem do RESEARCH.md "Standard Stack", verificadas via `pip index versions` para Python 3.10.11:

    Linha 1: `# Python 3.10.11`
    Linha 2: `PySide6==6.8.3`
    Linha 3: `opencv-python==4.10.0.84`
    Linha 4: `mediapipe==0.10.14`
    Linha 5: `pyvirtualcam==0.15.0`
    Linha 6: `obsws-python==1.7.2`
    Linha 7: `keyboard==0.13.5`
    Linha 8: `pygrabber==0.2`

    Nao adicionar nenhuma outra dependencia. Nao usar ranges (`>=`, `~=`) — apenas `==` exato (D-03). Nao deixar a linha invalida `python version == 3.10.11` em lugar nenhum do arquivo.
  </action>
  <verify>
    <automated>grep -v '^#' requirements.txt | grep -c '==' | grep -qx 7 && grep -qx 'pygrabber==0.2' requirements.txt && grep -qx 'mediapipe==0.10.14' requirements.txt && ! grep -q 'python version' requirements.txt && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contem exatamente 7 linhas de dependencia, todas com `==`
    - Linha 1 e o comentario `# Python 3.10.11`
    - `pygrabber==0.2` presente
    - `mediapipe==0.10.14` inalterado
    - A string `python version` NAO aparece em nenhuma linha do arquivo
    - Nenhuma dependencia usa range (`>=`, `~=`, `<`)
  </acceptance_criteria>
  <done>requirements.txt tem `# Python 3.10.11` + 7 deps pinadas com `==`, incluindo pygrabber==0.2 e mediapipe==0.10.14 intacto; a linha invalida foi removida.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validar install em venv limpo</name>
  <read_first>
    - requirements.txt (apos Task 2)
    - .planning/phases/01-foundation/01-VALIDATION.md (secao "Manual-Only Verifications" — linha pip install)
  </read_first>
  <action>Pedir ao usuario que rode `pip install -r requirements.txt` em venv limpo e confirme que conclui sem erro de resolucao nem wheel ausente. Nenhum codigo e escrito nesta task — e validacao do artefato da Task 2.</action>
  <what-built>
    O `requirements.txt` foi reparado com 7 dependencias pinadas. Como o ambiente de execucao do Claude pode ja ter pacotes instalados (mascarando conflitos), a unica prova de DEP-01/DEP-02 e um install limpo, que o usuario executa localmente.
  </what-built>
  <how-to-verify>
    1. Em um terminal na raiz do projeto, crie um venv limpo: `python -m venv test_env`
    2. Ative: `test_env\Scripts\activate`
    3. Rode: `pip install -r requirements.txt`
    4. Confirme que termina sem erro de resolucao de dependencia e sem erro de wheel ausente
    5. Confirme que pygrabber aparece em `pip list`
    6. Pode remover o venv apos: `deactivate` e apagar a pasta `test_env`
  </how-to-verify>
  <acceptance_criteria>
    - `pip install -r requirements.txt` completa sem erro em venv limpo
    - `pygrabber` aparece em `pip list`
    - Nenhum conflito de versao reportado entre opencv-python e mediapipe
  </acceptance_criteria>
  <resume-signal>Digite "approved" se o install passou, ou cole a mensagem de erro</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> ambiente venv | Pacotes baixados de registro externo cruzam para o ambiente local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pip installs (7 pacotes) | mitigate | Versoes pinadas com `==` impedem pull de versao maliciosa futura; pygrabber [ASSUMED] passa por checkpoint human-verify (Task 1) contra pypi.org antes de entrar no arquivo |
| T-01-01 | Tampering | requirements.txt | accept | Arquivo versionado no git; mudancas passam por commit do usuario (Winicius faz commits manualmente) |
</threat_model>

<verification>
- `grep -v '^#' requirements.txt | grep -c '=='` retorna 7
- `pip install -r requirements.txt` em venv limpo termina sem erro (verificado por humano — Task 3)
- A string `python version` nao aparece no arquivo
</verification>

<success_criteria>
- Roadmap Success Criterion 3: `pip install -r requirements.txt` em ambiente limpo instala todas as dependencias sem erros, incluindo pygrabber — SATISFEITO
- DEP-01 (pygrabber adicionado com versao fixada) e DEP-02 (todas as deps pinadas) cobertos
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

Nenhum novo simbolo de codigo (funcao/classe/modulo). Apenas modificacao de arquivo de configuracao:
- `requirements.txt` — reparado: 7 dependencias pinadas com `==` + comentario `# Python 3.10.11`; nova entrada `pygrabber==0.2`.
</artifacts_produced>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
