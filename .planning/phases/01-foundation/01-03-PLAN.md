---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - core/gesture_aliases.py
  - core/gesture_detector.py
  - engine/gesture_engine.py
  - ui/main_window.py
autonomous: false
requirements: [ENG-03]
must_haves:
  truths:
    - "Existe uma unica definicao de GESTURE_ALIASES, em core/gesture_aliases.py"
    - "gesture_detector.py, gesture_engine.py e main_window.py importam GESTURE_ALIASES do modulo canonico"
    - "O dict canonico contem a uniao de todos os gestos das 3 copias divergentes"
    - "O app inicia sem ImportError apos a consolidacao"
  artifacts:
    - path: "core/gesture_aliases.py"
      provides: "Fonte unica de verdade do dict GESTURE_ALIASES"
      contains: "GESTURE_ALIASES = {"
    - path: "engine/gesture_engine.py"
      provides: "Engine importa alias canonico"
      contains: "from core.gesture_aliases import GESTURE_ALIASES"
    - path: "ui/main_window.py"
      provides: "UI importa alias canonico"
      contains: "from core.gesture_aliases import GESTURE_ALIASES"
  key_links:
    - from: "engine/gesture_engine.py"
      to: "core/gesture_aliases.py"
      via: "import do dict canonico no topo do modulo"
      pattern: "from core\\.gesture_aliases import GESTURE_ALIASES"
    - from: "core/gesture_detector.py"
      to: "core/gesture_aliases.py"
      via: "GestureDetector.GESTURE_ALIASES referencia o dict canonico"
      pattern: "from core\\.gesture_aliases import GESTURE_ALIASES"
---

<objective>
Consolidar `GESTURE_ALIASES` em fonte unica de verdade (ENG-03, D-07/D-08/D-09). Hoje existem 3 copias DIVERGENTES do dict: `GestureDetector.GESTURE_ALIASES` (atributo de classe em core/gesture_detector.py, sem `"V"` e sem 3 gestos), `GESTURE_ALIASES` em engine/gesture_engine.py (modulo) e em ui/main_window.py (modulo). As copias da engine e da UI tem `"V"`, mas nenhuma das 3 inclui os gestos que `detectar()` retorna diretamente como display name: `"Escoteiro"`, `"Dedo do Meio"`, `"Arminha"`. Isso causa lookups inconsistentes de nome de gesto conforme o modulo consultado.

Purpose: ENG-03 — eliminar drift entre copias. `_normalize_gesture_name()` da engine usa `GESTURE_ALIASES.get(x, x)`; se o dict nao tem todas as chaves que `detectar()` produz, o mesmo gesto pode mapear para nomes diferentes em modulos diferentes (Roadmap Success Criterion 5: GESTURE_ALIASES retorna o mesmo dict independente do modulo).
Output: novo modulo `core/gesture_aliases.py` com o dict union; as 3 copias locais removidas e substituidas por import.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@core/gesture_detector.py
@engine/gesture_engine.py
@ui/main_window.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar core/gesture_aliases.py com o dict canonico (union)</name>
  <read_first>
    - core/gesture_detector.py (linhas 4-15 — copia atual, e o corpo de detectar() linhas 32-91 para confirmar todos os return strings)
    - engine/gesture_engine.py (linhas 18-30 — copia atual)
    - ui/main_window.py (linhas 51-63 — copia atual)
    - .planning/phases/01-foundation/01-RESEARCH.md (secao "Padrao 5" e "Code Examples > Exemplo 2", e "Common Pitfalls > Pitfall 4")
    - .planning/phases/01-foundation/01-CONTEXT.md (decisoes D-07, D-08, D-09)
  </read_first>
  <action>
    Criar o novo arquivo `core/gesture_aliases.py` contendo APENAS o dict `GESTURE_ALIASES` (sem funcoes helper — D-08) e um comentario de cabecalho de uma linha explicando que e a fonte unica. O dict e a UNIAO de todas as 3 copias mais os gestos que `GestureDetector.detectar()` retorna diretamente como display name. Os 14 pares chave->valor exatos sao:

    - "THUMBS_UP" -> "Joinha"
    - "THUMBS_DOWN" -> "Deslike"
    - "OPEN_HAND" -> "Mão aberta"
    - "FIST" -> "Punho"
    - "POINT" -> "Apontando p/ cima"
    - "ROCK" -> "ROCK"
    - "THREE" -> "TRES"
    - "FOUR" -> "QUATRO"
    - "OK_SIGN" -> "OK"
    - "CALL_ME" -> "Me liga"
    - "V" -> "V"
    - "Escoteiro" -> "Escoteiro"
    - "Dedo do Meio" -> "Dedo do Meio"
    - "Arminha" -> "Arminha"

    As 4 ultimas entradas (`V`, `Escoteiro`, `Dedo do Meio`, `Arminha`) sao passthrough: `detectar()` ja retorna essas strings como display name, entao `GESTURE_ALIASES.get(x, x)` retorna o proprio valor. Sem elas o dict ficaria incompleto (Pitfall 4). Seguir a convencao `UPPER_SNAKE_CASE` para o nome do dict de modulo. O modulo NAO deve importar nada (zero imports — modulo de dados puro).
  </action>
  <verify>
    <automated>python -c "from core.gesture_aliases import GESTURE_ALIASES; assert len(GESTURE_ALIASES) == 14, len(GESTURE_ALIASES); assert GESTURE_ALIASES['Escoteiro'] == 'Escoteiro'; assert GESTURE_ALIASES['V'] == 'V'; assert GESTURE_ALIASES['Arminha'] == 'Arminha'; assert GESTURE_ALIASES['Dedo do Meio'] == 'Dedo do Meio'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `core/gesture_aliases.py` existe e exporta `GESTURE_ALIASES`
    - `len(GESTURE_ALIASES) == 14`
    - Contem as 4 chaves passthrough: `V`, `Escoteiro`, `Dedo do Meio`, `Arminha`
    - O modulo nao tem nenhuma declaracao `import`
    - O modulo nao define funcoes (apenas o dict + comentario)
  </acceptance_criteria>
  <done>core/gesture_aliases.py existe com `GESTURE_ALIASES` de 14 entradas (union das 3 copias + 3 passthrough do detector), sem imports e sem funcoes helper.</done>
</task>

<task type="auto">
  <name>Task 2: Remover as 3 copias locais e importar do modulo canonico</name>
  <read_first>
    - core/gesture_detector.py (linhas 1-15 — atributo de classe GestureDetector.GESTURE_ALIASES a remover; verificar se ha referencias internas a self.GESTURE_ALIASES no arquivo)
    - engine/gesture_engine.py (linhas 1-30 — import block + copia de modulo a remover; linha 247 usa GESTURE_ALIASES.get)
    - ui/main_window.py (linhas 1-63 — import block + copia de modulo a remover; linhas 147 e 151 usam GESTURE_ALIASES.get)
    - core/gesture_aliases.py (criado na Task 1)
  </read_first>
  <action>
    Remover as 3 definicoes locais e substituir cada uma por `from core.gesture_aliases import GESTURE_ALIASES`. Detalhes por arquivo:

    1. `engine/gesture_engine.py`: deletar o bloco de modulo `GESTURE_ALIASES = {...}` (linhas 18-30, entre `logger = get_logger(__name__)` e `class GestureStabilityMonitor:`). Adicionar `from core.gesture_aliases import GESTURE_ALIASES` junto aos outros imports `from core.*` no topo (apos a linha 9 `from core.camera import CameraManager`). A referencia em `_normalize_gesture_name()` (linha 247) `GESTURE_ALIASES.get(gesture_name, gesture_name)` continua funcionando inalterada — agora resolve para o dict importado.

    2. `ui/main_window.py`: deletar o bloco de modulo `GESTURE_ALIASES = {...}` (linhas 51-63, entre `logger = get_logger(__name__)` e `RESOLUTION_PRESETS = {`). Adicionar `from core.gesture_aliases import GESTURE_ALIASES` junto aos imports (apos a linha 41 `from engine.gesture_engine import GestureEngine`). As referencias em `scene_map`/`bindings` comprehension (linhas 147, 151) `GESTURE_ALIASES.get(key, key)` continuam inalteradas.

    3. `core/gesture_detector.py`: remover o atributo de classe `GESTURE_ALIASES = {...}` (linhas 4-15) de dentro de `class GestureDetector`. Adicionar no topo do arquivo (apos `import math`, linha 1) `from core.gesture_aliases import GESTURE_ALIASES`. ATENCAO: se existir qualquer referencia a `self.GESTURE_ALIASES` ou `GestureDetector.GESTURE_ALIASES` no arquivo, trocar para o nome de modulo `GESTURE_ALIASES`. Conforme leitura, `detectar()` nao referencia o dict — apenas retorna strings — entao a remocao do atributo de classe e segura, mas confirmar via grep.

    NAO fazer `.copy()` do dict em nenhum modulo (Anti-Pattern do RESEARCH.md): importar e usar `GESTURE_ALIASES` diretamente para preservar a fonte unica.
  </action>
  <verify>
    <automated>test "$(grep -rc 'GESTURE_ALIASES = {' core/gesture_detector.py engine/gesture_engine.py ui/main_window.py | grep -c ':0')" = "3" && grep -q 'from core.gesture_aliases import GESTURE_ALIASES' core/gesture_detector.py && grep -q 'from core.gesture_aliases import GESTURE_ALIASES' engine/gesture_engine.py && grep -q 'from core.gesture_aliases import GESTURE_ALIASES' ui/main_window.py && python -c "import ast; [ast.parse(open(f, encoding='utf-8').read()) for f in ['core/gesture_detector.py','engine/gesture_engine.py','ui/main_window.py','core/gesture_aliases.py']]; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - Nenhum dos 3 arquivos (gesture_detector.py, gesture_engine.py, main_window.py) contem mais uma linha `GESTURE_ALIASES = {`
    - Os 3 arquivos contem `from core.gesture_aliases import GESTURE_ALIASES`
    - Os 4 arquivos compilam (ast.parse sem SyntaxError)
    - Nenhuma referencia a `self.GESTURE_ALIASES` ou `GestureDetector.GESTURE_ALIASES` permanece
    - Nenhum modulo faz `GESTURE_ALIASES.copy()`
  </acceptance_criteria>
  <done>As 3 copias locais foram removidas; gesture_detector.py, gesture_engine.py e main_window.py importam `GESTURE_ALIASES` de core.gesture_aliases; os 4 arquivos compilam.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificar que o app inicia e o alias e identico em todos os modulos</name>
  <read_first>
    - core/gesture_aliases.py
    - engine/gesture_engine.py (import block)
    - ui/main_window.py (import block)
  </read_first>
  <action>Pedir ao usuario que rode o comando de identidade de objeto e inicie `python main.py` para confirmar que os 3 modulos referenciam o mesmo dict e o app abre sem ImportError. Nenhum codigo e escrito nesta task — e validacao dos artefatos das Tasks 1 e 2.</action>
  <what-built>
    O dict `GESTURE_ALIASES` agora vive em `core/gesture_aliases.py` e e importado pelos 3 modulos. A prova final de ENG-03 (Roadmap Criterion 5: mesmo dict em qualquer modulo) e o app iniciar sem ImportError e o objeto ser o mesmo por identidade em runtime.
  </what-built>
  <how-to-verify>
    1. Na raiz do projeto, rode: `python -c "import engine.gesture_engine as e, ui.main_window as m, core.gesture_detector as d; print(e.GESTURE_ALIASES is m.GESTURE_ALIASES is d.GESTURE_ALIASES)"` — deve imprimir `True` (mesmo objeto por identidade)
    2. Inicie o app: `python main.py`
    3. Confirme que a janela abre sem traceback de ImportError no console
    4. Confirme que a aba de gestos lista os gestos com os nomes corretos (Joinha, Mão aberta, etc.)
  </how-to-verify>
  <acceptance_criteria>
    - O comando de identidade imprime `True` (os 3 modulos referenciam o MESMO objeto dict)
    - `python main.py` abre a janela sem ImportError
    - Os nomes de gesto na UI estao corretos
  </acceptance_criteria>
  <resume-signal>Digite "approved" se o app iniciou e a identidade deu True, ou cole o erro</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (interno) | Nenhuma fronteira de confianca cruzada — refatoracao puramente interna de constantes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | drift entre copias de GESTURE_ALIASES | mitigate | ENG-03: fonte unica em core/gesture_aliases.py; impede que copias divirjam silenciosamente e produzam mapeamento de gesto inconsistente |
</threat_model>

<verification>
- `from core.gesture_aliases import GESTURE_ALIASES` resolve e `len == 14`
- `grep 'GESTURE_ALIASES = {'` retorna 0 ocorrencias nos 3 arquivos editados
- Identidade de objeto entre os 3 modulos imprime `True` (verificado por humano — Task 3)
- `python main.py` inicia sem ImportError
</verification>

<success_criteria>
- Roadmap Success Criterion 5: GESTURE_ALIASES retorna o mesmo dicionario independente do modulo consultado — SATISFEITO (identidade de objeto)
- ENG-03 coberto: 3 copias divergentes eliminadas, dict union em fonte unica
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces

Novos simbolos:
- `core/gesture_aliases.py` (novo modulo) — define `GESTURE_ALIASES` (dict de 14 entradas), fonte unica de verdade.

Simbolos removidos:
- `GestureDetector.GESTURE_ALIASES` (atributo de classe em core/gesture_detector.py) — removido.
- `GESTURE_ALIASES` (constante de modulo em engine/gesture_engine.py) — removido, substituido por import.
- `GESTURE_ALIASES` (constante de modulo em ui/main_window.py) — removido, substituido por import.
</artifacts_produced>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
