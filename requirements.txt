# Python 3.10.11
PySide6==6.8.3
opencv-python==4.10.0.84
mediapipe==0.10.14
pyvirtualcam==0.15.0
obsws-python==1.7.2
keyboard==0.13.5
pygrabber==0.2

# Captura de câmera via FFmpeg/DirectShow (core/camera.py).
# Pin exato: 14.2.0 é a última release com wheel cp310 para win_amd64. Da 14.3 em
# diante o PyAV só publica sdist para o 3.10, e o pip cai no build from source, que
# exige MSVC Build Tools. Fecha a pendencia do D-24; ver D-25 e ENV-01.
av==14.2.0

# Dependência transitiva do mediapipe, declarada só para travar a versão. Os dois
# pacotes de OpenCV instalam no MESMO diretório `cv2`, e o último a instalar vence:
# sem este pin o contrib resolve para 5.0.0.93 e passa a responder no lugar do
# opencv-python==4.10.0.84 acima, tornando aquele pin inócuo. Mesma versão upstream
# nos dois, portanto, independente da ordem de instalação. Ver D-25 e ENV-02.
opencv-contrib-python==4.10.0.84

# Importado diretamente em integrations/obs_connect_thread.py (WebSocketTimeoutException).
# Chega como dependência transitiva do obsws-python; declarado aqui porque o import é
# direto. Sem pin exato para não conflitar com a resolução do obsws-python.
websocket-client>=1.6
