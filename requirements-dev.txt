# Dependências de desenvolvimento — não vão para o executável.
# Instale com: pip install -r requirements.txt -r requirements-dev.txt
#
# Pins derivados de uma instalação real e verificada, não de palpite (ver D-24/D-25).

pytest==9.1.1
pyinstaller==6.22.2
